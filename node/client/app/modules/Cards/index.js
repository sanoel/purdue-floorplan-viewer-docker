export default module => {

  // Use lower case, with dash line to separate words, for naming the variables
  // in the cerebral state.
  module.addState({
    // Each element is an object specifying... TODO: Complete this.
    cards_to_show: []
  })

  module.addSignals({

  })

}
